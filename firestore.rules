rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인한 본인의 기록만 읽고 쓸 수 있습니다.
    match /users/{userId}/garments/{docId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
